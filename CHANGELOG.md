# История изменений

Каждая запись указывает версию Orca, из которой сняты английские строки.

После 1.4.0 версия пакета повторяет версию Orca один в один: каталог под
Orca 1.4.204 выходит как 1.4.204 (см. «Версионность» в
[README](README.md#версионность)).

## 1.4.204 — 2026-09-16 · Orca 1.4.204 (совместим с 1.4.169+)

Каталог — 14 090 из 14 279 строк (98%). Приложение добавило 20 ключей,
убрало три и переписало текст у ни одного — переведены все.

- **Перетаскивание файлов во встроенный чат** — «Отпустите, чтобы прикрепить
  к этому чату», «Файлы добавляются в сообщение как пути, которые агент
  может прочитать», ошибки прикрепления: «Не удалось прочитать перетащенные
  файлы», «Файлы можно прикрепить только к их исходной рабочей области»,
  «Во время прикрепления рабочая область сменила хост — перетащите файлы
  ещё раз», «Сначала сохраните их на диск, затем перетащите сохранённые файлы»
- **Настройка «Убирать поле при копировании»** в разделе терминала —
  описание про левое поле, за которым рисуется вывод агента, и ключевые слова
  поиска «поле», «отступ», «ведущие», «пробелы», «пробельные символы»
- **Проекты GitHub** — пустое представление: «В этом представлении пока нет
  элементов», «Недавно добавленные элементы могут появиться не сразу»
- Значение опции в композере «Не сообщено» вместо убранных «Вкл.» / «Выкл.»
  и «Текущее значение неизвестно»
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.203 — 2026-09-15 · Orca 1.4.203 (совместим с 1.4.169+)

Каталог — 14 073 из 14 262 строк (98%), без изменений относительно 1.4.201:
приложение не добавило, не убрало и не переписало ни одной строки. Релиз
1.4.202 у Orca не выходил. Выпуск нужен только для совпадения номера
пакета с версией приложения.

## 1.4.201 — 2026-09-14 · Orca 1.4.201 (совместим с 1.4.169+)

Каталог — 14 073 из 14 262 строк (98%). Приложение добавило 54 ключа,
убрало три и переписало текст у одного — переведены все.

- **Фоновые задачи во встроенном чате** — счётчики по типам: «агентов: N»,
  «shell: N», «мониторов: N», «сценариев: N», «задач: N», «фоновых задач: N»;
  состояния «наблюдение», «заблокирован», причины «нужно одобрение»
  и «нет связи», группа «Мониторы»
- **Состояния агентов переведены** — `working`, `waiting`, `done` раньше
  стояли в словаре тождественно как идентификаторы; с этой версии Orca
  показывает их как подписи в фоновых задачах и уведомлениях, поэтому
  теперь «работает», «ожидает», «готово». Уведомления: «нужен ввод»,
  «завершён»
- **Карточка «Вы вышли из аккаунта»** — что вернётся после повторного входа:
  публикация артефактов, Orca Relay, обмен навыками
- «Показать в Finder», «Показать в проводнике», «Открыть содержащую папку»
  в контекстных меню вкладок, проводника и заголовка редактора
- Предупреждения удаления рабочей области с незакрытыми сессиями агентов
  переписаны под новые тексты Orca
- Исправлено согласование в трёх старых переводах: «Git уже удалил эту
  рабочую область», «Git уже забыл эту рабочую область, но её каталог…»
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.200 — 2026-09-11 · Orca 1.4.200 (совместим с 1.4.169+)

Каталог — 14 022 из 14 211 строк (98%). Приложение добавило 23 ключа,
ничего не убрало и не переписало — переведены все.

- **Список задач во встроенном чате** — «Выполнено задач: N из M», события
  «Добавлена задача», «Начата задача», «Выполнена задача», «Возвращена
  в ожидание», «Полный список задач», «Задачи без изменений»
- **Дифф хода** — «Изменённых файлов: N» с формами числа, «Частичный дифф»,
  итоги по записанным правкам
- **Квитанции ответов** — «Отвечено на {{device}}», «Отменено на {{device}}»,
  «Выбранный ответ недоступен»
- Формы числа для нового счётчика изменённых файлов — теперь 52 ключа,
  166 записей
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.199 — 2026-09-10 · Orca 1.4.199 (совместим с 1.4.169+)

Каталог — 13 999 из 14 188 строк (98%). Приложение добавило 97 ключей,
убрало 8 и переписало текст у четырёх — переведены все.

- **MiniMax: API-ключ и выбор эндпоинта** — международный
  (platform.minimax.io) или китайский (platform.minimaxi.com), API-ключ
  с приоритетом над cookie, «Забыть ключ», истёкшие ключ и cookie
  в подсказке статус-бара, ключевые слова поиска по настройкам
- **Контраст цветов терминала** — «Автоматически», «Выкл.», целевой контраст
  «Слабый» / «Сильный», описания режимов, ключевые слова поиска
- **Субагенты во встроенном чате** — «Запущено субагентов: N»,
  «Выполнено субагентов: N», состояния «завершён», «ошибка», «остановлен»,
  «не проверен», счётчики работающих и простаивающих, токены
- **Продолжение сессии в новом чате** из панели AI Vault — «Продолжить
  в новом чате», диалог уже открыт в другом чате, история не загрузилась
- Уведомления чата «Контекст сжат» и «План», коды выхода и миллисекунды
  у инструментов, «Ожидает ввода» и «Давно без обновлений» в активности,
  Grok без процента расхода, Relay по сотовой сети в сопряжении с мобильным
- Настройка действий для ссылок переписана: теперь охватывает и расшифровку
  чата, не только терминал
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.198 — 2026-09-08 · Orca 1.4.198 (совместим с 1.4.169+)

Каталог — 13 910 из 14 099 строк (98%). Приложение добавило 121 ключ,
убрало два и переписало текст у семи — переведены все.

- **Передача сессии между чатом и терминалом** — стадии «Завершение сессии
  чата…», «Открытие терминала агента…», «Восстановление сессии агента…»,
  «Переключиться после этого хода», «Вернуться в чат», «Открыть TUI агента»
- **Фоновые задачи во встроенном чате** — фоновый агент, сценарий, команда,
  монитор; «Наблюдение за фоновыми задачами», «Остановить фоновые задачи»
- **Дорожная карта проектов GitHub** — квартал, год, масштаб ленты,
  «Размещено по {{value0}}», «Без дат», подсказки про неподдерживаемые макеты
- **Восстановление после сбоя окна** — «Orca не удаётся загрузиться», счётчик
  попыток, подсказка про графический драйвер
- Разделение чата вправо и вниз в быстрых действиях, скрытая рабочая область
  и сброс фильтров боковой панели, повтор сканирования worktree, подсказки
  тем на онбординге («Как в системе», «Не напрягает глаза», «Светлая и чёткая»),
  возможности интеграции с GitHub, французский в списке языков
- Структурированный чат теперь для Codex и Claude — тексты настройки
  переписаны, ошибки открытия и закрытия чата получили имя провайдера
- **Добор из `app.asar` стал нулевым**: с 1.4.198 ключи поиска по настройкам
  приходят через `en.json` после слияния PR stablyai/orca#18040. Разбор
  бандла оставлен как страховка
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.197 — 2026-09-04 · Orca 1.4.197 (совместим с 1.4.169+)

Каталог — 13 791 из 13 980 строк (98%). Приложение добавило 86 ключей,
убрало два и переписало текст у 10 — переведены все.

- **Вкладка «Агенты» в боковой панели** — знакомство с новой вкладкой,
  переезд из панели агентов, «Пространства» и «Вид боковой панели»
  в заголовке, параметры активности: только непрочитанные, дочерние
  агенты, компактные треды, «Убрать завершённые»
- **Панель запусков автоматизаций** — поиск и обновление запусков,
  счётчики «Успешных · 24 ч» / «Неудачных · 7 дн.», подробности запуска,
  сообщение о недоступной истории с формами числа
- **Апдейтер** — «Скачать обновление» вместо «Установить», отдельные тексты
  для Orca из системного менеджера пакетов, карточка «Требуется установка
  вручную» для Linux-пакета переписана
- **Терминалы** — переподключение с восстановлением вывода, хост не смог
  проверить сохранённую сессию, недоступные удалённые терминалы в SSH
- Пробелы в диффе: настройка «Пробелы в диффе», переключатель «Пробелы:
  вкл./выкл.», «Показывать пробелы» в меню Markdown; копирование ID сессии
  из контекстного меню терминала; сохранение вставленного изображения
- Формы числа для двух новых счётчиков — теперь 51 ключ, 162 записи
- Исправлено согласование в старых переводах: «плавающая рабочая область»
  во всех падежах вместо «плавающее рабочая область», «текущая рабочая
  область», «2 вложенные рабочие области»
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.196 — 2026-09-03 · Orca 1.4.196 (совместим с 1.4.169+)

Каталог — 13 707 из 13 896 строк (98%). Приложение добавило 26 ключей,
убрало два и переписало текст у одного — переведены все.

- **Сводки хода агента во встроенном чате** — «Выполняется команда»,
  «Выполняется {{toolName}}», итог «Выполнено команд: N, использовано
  инструментов: M», таймер «В работе N с» / «Работа заняла N с»,
  переключатель подробностей хода
- **Среда выполнения Orca недоступна при живом SSH** — новое состояние
  в статус-баре и в настройках удалённых сред: транспорт подключён,
  но среда выполнения не ответила
- Подсказка омнибокса теперь упоминает историю: «Поиск по вкладкам,
  истории, файлам, адресам и агентам…», пункт «Открыть страницу»
- Дифф неизвестного размера загружается по запросу, автоматизация
  сменила хост, артефакт слишком велик для публикации (вместо прежнего
  лимита 800 КБ)
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.195 — 2026-09-02 · Orca 1.4.195 (совместим с 1.4.169+)

Каталог — 13 683 из 13 872 строк (98%). Приложение добавило 38 ключей
и убрало два — переведены все.

- **Отвязка пул-реквеста от рабочей области** — пункт меню, состояние
  «Пул-реквест отвязан» в панели проверок, статус в контроле версий
- **Состояние хоста в статус-баре** — проверка доступности, восстановление
  подключения, время последнего подключения
- Проверка SSH (verification challenge), большие диффы не отрисовываются
  по умолчанию, предупреждение про проект на диске Windows под WSL,
  выбор пользователя Jira
- Формы числа для нового счётчика «неразрешённых конфликтов исключено» —
  теперь 49 ключей, 154 записи
- `extract.py` переписан под минифицированный бандл 1.4.195: `translate` стал
  однобуквенным алиасом, строки ушли в обратные кавычки — регулярки ловят оба
  вида кавычек и любое имя функции. Без этого список ключей с `count` пустел
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.194 — 2026-09-01 · Orca 1.4.194 (совместим с 1.4.169+)

Каталог — 13 649 из 13 838 строк (98%). Приложение добавило 10 ключей
и переписало текст у двух — переведены все.

- **Охват фильтра задач Linear** — предупреждения о том, что часть команд
  в фильтр не попала: сколько меток и статусов реально фильтруется, отметка
  «· частично» у затронутых разделов
- **Автоматизации** — переписаны тексты про недоступную историю запусков
  и про хост, которому нужно обновление
- Правка адреса в предпросмотре HTML, «Открыть в браузере Orca» у карточки
  worktree, предупреждение о переполненной очереди вложений в чате
- `partial` в статусе SSH-подключения переведено как «частично» — рядом
  «подключено» и «конфликт» уже были по-русски
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.193 — 2026-08-31 · Orca 1.4.193 (совместим с 1.4.169+)

Каталог — 13 639 из 13 828 строк (98%). Приложение добавило 43 ключа
и переписало текст у одного — переведены все.

- **Чек-лист первичной настройки** — карточки с задачами: добавить репозитории,
  выбрать агента по умолчанию, работать в двух worktree сразу, включить
  уведомления, подключить интеграции, автоматизировать подготовку worktree
- **Структурированный нативный чат** — экспериментальная среда выполнения Codex
  на стороне хоста: настройки, границы применимости, ошибки открытия и закрытия,
  сообщение о поставленной на паузу оркестрации
- **GitLab в панели проверок** — связать и отвязать merge-реквест, ссылки MR
  в сведениях о worktree
- «Пометить готовым к ревью» для черновиков пул-реквестов
- **Русские формы числа.** У 66 ключей приложение передаёт `count`, не зашивая
  суффикс в сам ключ, — там форму выбирает i18next по правилам языка и просит
  `_few` и `_many`, которых в английском каталоге нет. Такие формы добавлены
  для 48 ключей (150 записей): «1 навык», «2 навыка», «5 навыков», «21 навык»,
  «Удалить 11 объектов?». Остальные 18 кандидатов не нуждаются в склонении
  («{{count}} PR», «{{count}} д»)
- `tools/extract.py` собирает список ключей с `count` в `plural-candidates.txt`,
  `tools/build.py` подмешивает формы из `dict/plurals.json`, проверяет их
  плейсхолдеры и предупреждает, если форма описана для ключа, которому `count`
  не передают

## 1.4.192 — 2026-08-30 · Orca 1.4.192 (совместим с 1.4.169+)

Каталог — 13 596 из 13 785 строк (98%). Приложение добавило 48 ключей,
убрало 5 и переписало текст у пяти — переведены все.

- **Родительский worktree при создании** — выбор родителя прямо в композере
  и предупреждение, если вложить не удалось
- **Перенос автоматизации на другой хост** — предупреждение о том, что
  оригинал вместе с историей запусков будет удалён, и разбор случаев,
  когда удалить его не вышло
- **Установка навыков** — все состояния и ошибки: проверка доступа к пакету,
  недоступная публикация, сверка версии, история версий
- **Глубина вложенности воркеров** в настройках оркестрации
- Копирование вывода, заданий и аннотаций проверок; описания метрик памяти
  working set и private bytes; режим исходника для больших markdown-файлов
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.191 — 2026-08-29 · Orca 1.4.191 (совместим с 1.4.169+)

Каталог — 13 553 из 13 742 строк (98%). Приложение добавило 339 ключей,
убрало 8 и переписало текст у двух — переведены все.

- **Автоматизации по хостам** — выбор хоста при создании, статусы источника
  данных, выполнения и запросов, пустые состояния и уведомления о недоступных
  или удалённых хостах, история запусков
- **Удалённый просмотр в браузере** — страницы удалённых рабочих областей
  теперь могут отрисовываться на этом устройстве, а трафик идти через хост:
  настройки маршрутизации, состояния страниц на другом устройстве, ошибки
  проброса TCP на SSH-сервере, «Открыть заново на сервере»
- **Предпросмотр HTML-документов** — запросы доступа к папкам, ограничения
  на размер файлов, копирование путей, блокировка загрузок
- **Удаление навыков** — выбор, причины запрета, итоги операции; выбор
  целевых агентов при установке
- **Очистка рабочих областей** — чипы применённых фильтров, риск и
  принудительное удаление, недоступный статус git
- Задачи GitHub, GitLab, Linear и Jira, сравнение ветки с базой
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.190 — 2026-08-27 · Orca 1.4.190 (совместим с 1.4.169+)

Каталог — 13 222 из 13 411 строк (98%). Приложение добавило 27 ключей —
переведены все. Версия 1.4.189 пропущена: перевод собран сразу с 1.4.190.

- **Расписания автоматизаций** — человекочитаемые подписи («Ежедневно в …»,
  «По будням в …», «Ежечасно в :…», «Своё расписание») и недостающие поля
  панели cron: «Месяц», «День недели», «Cron корректен»
- **Диктовка** — состояния индикатора: запуск микрофона, «Слушаю», «Говорите»,
  «Слишком громко», «Обработка…»
- **Завершение терминала** — оверлей с кодом выхода и отдельный случай предела
  в 128 консолей Git Bash
- Ошибка переименования группы worktree, состояния списка агентов в настройках
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.188 — 2026-08-22 · Orca 1.4.188 (совместим с 1.4.169+)

Каталог — 13 195 из 13 384 строк (98%). Приложение добавило 10 ключей —
переведены все.

- Возраст сессии в палитре ⌘J: «<1 мин», «{{value0}} мин», «{{value0}} ч»,
  «{{value0}} д»
- «Последняя активность … назад» в палитре перехода по worktree
- Подпись проекта на SSH-хосте в боковой панели
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.187 — 2026-08-22 · Orca 1.4.187 (совместим с 1.4.169+)

Каталог — 13 185 из 13 374 строк (98%). Самый крупный релиз с начала проекта:
534 новых ключа, 11 убрано, у четырёх переписан текст — переведены все.

- **Обмен навыками по ссылке** — раздел целиком: публикация одного навыка или
  набора за скрытой ссылкой, ревью состава файлов и скриптов перед публикацией,
  неизменяемые версии, управление доступом и отзыв ссылок, отдельная страница
  настроек «Поделиться навыками»
- **Установка навыков по ссылке** — просмотр перед установкой, выбор части
  набора, решения по локальным копиям, выбор компьютера и места установки,
  управление установленными навыками с откатом версий
- **Расположение проекта** — диалог выбора папки или клонирования на хост
- Каналы сборок: предупреждения про неподписанные сборки под Windows
- Импорт cookie: подробные причины, по которым часть cookie не расшифровалась
- Passkey в браузере, аннотации, конфликты rebase и cherry-pick, мелочи GitHub
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.186 — 2026-08-21 · Orca 1.4.186 (совместим с 1.4.169+)

Каталог — 12 662 из 12 851 строк (98%). Приложение добавило 47 ключей,
убрало один и переписало текст у одного — переведены все.

- **Внешние worktree** — глобальная настройка видимости и переопределение
  для отдельного проекта: «Использовать глобальное», «Переопределяет
  глобальную настройку», подсказки в диалоге источников
- **Остановка очистки облачной VM** — кнопка, диалог подтверждения
  и предупреждение о том, что машина может остаться запущенной
- Вход в Codex из статус-бара и сообщение о временно недоступных файлах Codex
- Конфликт сочетания клавиш с Mission Control
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.185 — 2026-08-20 · Orca 1.4.185 (совместим с 1.4.169+)

Каталог — 12 616 из 12 805 строк (98%). Крупный релиз приложения: 219 новых
ключей, 13 убрано, у шести переписан текст — переведены все.

- **Разбор рабочих областей для очистки** — новый фасетный браузер: фильтры
  по git, размеру на диске, простою, тикетам, ревью и блокировкам, сортировки,
  сканирование занятого места, отчёты о найденном
- **Источники видимости worktree** — список источников (корень worktree, GSD,
  свои расположения), включение источника целиком и показ отдельных worktree,
  строка о новых скрытых worktree в боковой панели
- **Карта агентов** — управление картой в отдельном окне: быстрые виды
  («Требуют внимания», «Застряли», «Залежались»), диапазоны времени в состоянии
- **Насыщенность жирного шрифта** в терминале
- Предупреждение о том, что разрешения macOS могут не дойти до терминалов Orca,
  если демон запущен предыдущей установкой
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.184 — 2026-08-17 · Orca 1.4.184 (совместим с 1.4.169+)

Каталог — 12 410 из 12 599 строк (98%). Приложение добавило 40 ключей,
убрало 46 и переписало текст у четырёх — переведены все новые.

- **Видимость worktree** — диалог и справка о том, какие worktree скрыты
  по умолчанию: режим «Показывать всегда», выбор по одному, worktree агентов
  в `.claude/worktrees/*` и `.gsd-workspaces/*`
- **Создание worktree из задачи** — палитра перехода теперь умеет заводить
  worktree по задаче Linear или ссылке на задачу
- **Фильтры автоматизаций** — «Успешно», «Не запускалась», сортировка списка
- Подписи хостов в отдельном окне дашборда
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.183 — 2026-08-15 · Orca 1.4.183 (совместим с 1.4.169+)

Первый выпуск по новой схеме: номер пакета равен версии Orca, из которой сняты
строки. Каталог — 12 416 из 12 605 строк (98%), приложение добавило 63 ключа
и убрало один.

- **Bitbucket Cloud** — интеграция целиком: диалог подключения (email с
  API-токеном Atlassian либо токен доступа), карточка в списке интеграций,
  приоритет переменных `ORCA_BITBUCKET_*`, ошибки аутентификации и отключение
- **Стеки пул-реквестов** — создание PR поверх существующего стека: кнопки
  «Создать PR в стеке», черновик и push, выбор базовой ветки
- **Импорт cookie браузера** — предупреждение о том, что входы в Google
  не переносятся, и что делать вместо этого
- Обсуждения на сервисе, поиск по быстрым командам, кнопка «Перейти наверх»
  в списке worktree, сообщение об упавшем демоне терминала
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.4.0 — 2026-08-14 · Orca 1.4.182 (совместим с 1.4.169+)

Каталог пересобран: 12 354 из 12 543 строк (98%). Приложение добавило
110 ключей и убрало 171 — переведены все новые.

- **Бодрствование** — новый сегмент статус-бара и настройка «Не давать
  компьютеру заснуть»: режимы «Вкл.», «Агент», «Выкл.» и их описания
- **Разбор сохранённых веток** — диалог массового удаления локальных веток,
  которые Git оставил из-за возможных невлитых коммитов, и его уведомления
- **Действия для ссылок в терминале** — всплывающее меню (папка, Finder,
  браузер Orca или системный, переключение терминала и рабочей области)
  и настройка его показа
- **Реакции на комментарии GitHub** — добавление и снятие
- Фильтр рабочих областей, созданных другими клиентами Orca на общих серверах
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути

## 1.3.0 — 2026-08-12 · Orca 1.4.180 (совместим с 1.4.169+)

Каталог пересобран под Orca 1.4.180: 12 415 из 12 604 строк (98%).
С 1.4.176 приложение добавило 316 ключей и переписало текст ещё у четырёх —
переведены все.

- **Артефакты** — новый раздел целиком: публикация файлов HTML и Markdown по
  публичным ссылкам, боковая панель со списком, настройки публикации, диалоги
  удаления и все состояния кнопки «Опубликовать как артефакт»
- **Аккаунт Orca** — вход, выход, состояние сессии, описание облачных возможностей
- **Стеки пул-реквестов GitHub** — карта стека, причины блокировки слияния,
  подтверждения слияния и постановки в очередь целым стеком
- **Сессии AI Vault** — удаление и причины, по которым сессия удалению не подлежит
- **Быстрые команды на хостах** — загрузка с выбранного хоста Orca и её ошибки
- **Автоматизации** — пошаговое описание в настройках: работа, запуск, результаты
- Защищённая зона не изменилась: те же 180 ключей и 34 разрешённых пути
- `description` в манифесте сокращён до одной строки: подробное описание
  обрезалось в карточке плагина. Детали — в README

## 1.2.0 — 2026-08-09 · Orca 1.4.176 (совместим с 1.4.169+)

Защита от поломки у тех, кто обновил пакет, но не обновил Orca.

Пакет доходит до пользователя раньше, чем новая версия приложения, поэтому каталог
обязан быть валидным для всего диапазона версий. Опасен один класс изменений:
путь, разрешённый к переводу в новой Orca, но ещё защищённый в старой, —
он отклоняет каталог целиком, и язык молча выключается.

- `tools/extract.py` собирает `allowed-chrome.txt` **пересечением** белых списков
  минимальной поддерживаемой версии (файл `COMPAT`) и установленной
- `engines.orca` поднят с `>=1.4.0` до `>=1.4.169`: до 1.4.169 белого списка не
  существовало, вся зона `settings.plugin*` была закрыта, и текущие 34 разрешённых
  пути сломали бы пакет. Теперь Orca откажет в установке вместо тихой поломки
- Переведены 56 строк, добавленных в Orca 1.4.177 (статусы merge request GitLab,
  статистика кода, диспетчер ресурсов) — каталог 1.4.177 покрыт заранее полностью

## 1.1.1 — 2026-08-08 · Orca 1.4.176

Исправление: пакет не запускался («The plugin stopped after an activation or
worker error»).

Валидатор Orca отклоняет **весь каталог**, если хоть одно значение длиннее
8192 символов. Переход на полный `en.json` в 1.1.0 занёс в каталог два
CSS-блока витрины возможностей — 9691 и 9965 символов. Прежний разбор `asar`
их не видел: регулярка обрезала значения на 300 символах.

- `tools/build.py` пропускает строки сверх лимита и показывает их в отчёте;
  сборка падает, если превышает лимит сам перевод
- Переведено 12 103 из 12 292 (98%)

## 1.1.0 — 2026-08-08 · Orca 1.4.176

Покрытие выросло с 84% до 98% реального каталога, исправлены видимые дефекты перевода.

**Источник строк.** `tools/extract.py` больше не разбирает минифицированный
`app.asar` в одиночку: он скачивает `en.json` из репозитория Orca по тегу
установленной версии. Разбор бандла остался как дополнение — из него берутся
109 ключей поиска по настройкам, которых в `en.json` нет.

Прежняя регулярка находила только ключи с префиксом `auto.` и пропускала 1854 из
12 292 — то есть заявленные «98%» считались от неполного набора. Реальное
покрытие было 84%.

- **+1847 переведённых строк**: 12 105 из 12 292 (98%)
- Английский суффикс множественного числа больше не протекает в интерфейс:
  `{{value0}} label{{value1}}` давал «меток: 3s» — исправлено в 19 строках,
  сборка теперь отличает такой суффикс от настоящего плейсхолдера и проверяет его
- Исправлены переводы названий продуктов: `Cursor` показывался как «Курсор»,
  `Continue` — как «Продолжить». Добавлен механизм переопределения по ключу
  (`tools/dict/by-key.json`), потому что плоский словарь не различает контекст
- `idle` в трёх местах возвращён к английскому — это состояние, а не текст;
  официальные локали Orca его тоже не переводят
- Терминология выровнена по глоссарию smwbev/orca-russian:
  `workspace` → «рабочая область» (было «рабочее пространство»),
  `runtime` → «среда выполнения». Затронуто 450 строк, род согласован

## 1.0.1 — 2026-08-07 · Orca 1.4.176

- Сверено с Orca 1.4.176: набор строк не изменился, каталог собрался без расхождений
- `tools/extract.py` пишет `ORCA_VERSION` из `Info.plist` того же бандла,
  `tools/build.py` показывает версию в отчёте — версия фиксируется автоматически

## 1.0.0 — 2026-08-07 · Orca 1.4.175

Первый релиз: полная русификация интерфейса.

- **10 258 из 10 438 строк (98%)** — все разделы приложения
- 180 строк защищённой зоны Orca намеренно оставлены английскими
  (тексты про доверие к плагинам — их подмена запрещена валидатором)
- 669 технических литералов оставлены в оригинале: команды, пути,
  имена агентов, плейсхолдеры шаблонов
- Плейсхолдеры (`{{value0}}`, `{{host}}`, `{{count}}`) проверены на совпадение
  с оригиналом — расхождений нет
- Инструменты: `tools/extract.py` (извлечение строк из app.asar),
  `tools/build.py` (сборка каталога с проверками и защитой)
